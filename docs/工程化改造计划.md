# CleaningCar 工程化改造计划

## 1. 文档定位

本文档用于持续跟踪 CleaningCar 从“可运行原型”向“可交付、可复核、可维护产品”的工程化改造过程。

执行规则：

- 每次只实施一个未完成事项。
- 实现者完成代码和验证后只填写证据，不自行勾选。
- 只有项目负责人完成人工确认后，才能将 `[ ]` 改为 `[x]`。
- 未经明确授权，不启动现场推理、不修改甲方接口、不清理板端数据、不提交或推送 Git。
- 主路解码口径、RGA 禁区和“一车一次冲洗”产品语义属于硬约束，不得在普通优化中改变。

每个任务完成时使用以下记录模板：

```text
状态：待确认
完成日期：
关联 commit：
自动测试：
RK3588/现场验证：
遗留问题：
确认人：
```

## 2. 设备地图

### 2.1 校园网设备

| 项目 | 内容 |
| --- | --- |
| SSH 别名 | `RK3588` |
| 地址 | `10.200.98.128` |
| SSH 端口 | `22` |
| 用户 | `hinlink` |
| 用途 | 校园网内 RK3588 开发与验证 |
| 已使用目录 | `/home/hinlink/20260830` |

### 2.2 2号设备办公室

| 项目 | 内容 |
| --- | --- |
| SSH 别名 | `RK3588-Office-02` |
| 兼容别名 | `CleaningCar-RK3588-Cloud` |
| 地址 | `47.105.123.104` |
| SSH 端口 | `6006` |
| 用户 | `root` |
| 密钥标识 | `id_rsa_cleaningcar_47_105_123_104`，不在仓库保存密钥内容 |
| 设备型号 | RK3588，设备树型号 `ztl, A588` |
| 系统 | Ubuntu 22.04.5 LTS，aarch64 |
| 生产旧副本 | `/home/serve/cleaningcar_v2`，非 Git 目录，保留现场数据和旧运行环境 |
| 最新 Git 副本 | `/home/serve/cleaningcar_v2_latest` |
| Git 分支 | `embedded-wheel-optimization` |
| 当前基线 commit | `d01af8cf8ada3866107aca84fce073d97dfa9e92` |
| 板端 Web 端口 | `8000` |
| 外部映射 | `http://47.105.123.104:50006/` |

安全约定：

- 本文档和仓库不得记录 SSH 密码或私钥内容。
- 生产旧副本不得直接覆盖；迁移前必须停止相关进程、备份现场配置和运行数据，并单独取得授权。
- 最新 Git 副本可用于代码核验和测试，但未经授权不得启动推理连接现场 RTSP。

## 3. 当前基线

### 3.1 性能与运行状态

- 主视频输入约 `20 FPS`。
- 当前模型检测及后续处理通常为 `17～19 FPS`。
- 第一阶段以稳定性和事件正确性为优先目标，暂不要求强行追满 `20 FPS`。
- 办公室2号设备的 GStreamer `mppvideodec` 已确认支持 `BGR` 和 `DMABuf` 输出。
- 四个 RKNN 模型与办公室设备生产旧副本逐文件一致：主检测、车牌检测、车牌识别/颜色、车轮检测。

### 3.2 自动测试基线

- 板端现有 `venv-gst` 可导入 `numpy`、`cv2`、`fastapi`、`pydantic` 和 `rknnlite`。
- 板端未安装 `pytest`，现有25个测试文件均基于标准库 `unittest`，可直接运行。
- 完整测试共 `191` 项：`189` 项通过，`2` 项失败。
- 两项失败来自同一个旧断言：`test_site_config_runtime.py` 仍期望 `video.decode_backend=auto`，而两份现场配置已经固定为 `gstreamer`。
- 跟踪8项、事件36项、解码21项、Web 20项、车轮51项分类测试均通过。

### 3.3 已知架构问题

- 事件状态在 `track_timeout_frames=80` 时删除，但 ByteTrack 在 `track_max_age=100` 后才删除轨迹，可能让同一个 tracker ID重新建立空事件状态。
- 业务事件身份直接依赖短期 tracker ID，缺少跨ID、跨遮挡和跨进程重启的车辆生命周期。
- 新目标只要首次出现在 Zone A 内，就可能被当作新进场车辆。
- 水流判断目前没有可靠绑定到具体车辆，画面中的任意水流框可能影响多个已进入清洗区的轨迹。
- 当前上报采用 type1～type6 多阶段请求，必须确保同一车辆始终使用同一个业务ID，并由甲方按ID更新同一条记录。
- 当前 Web 主要是配置和运维后台，尚无面向甲方的事件台账、复核、纠错和审计能力。
- 2026-08-30 双路实测中的52条绕行事件经人工复核属于纯模型误检，可能同时受困难负样本不足和类别阈值偏低影响。
- 绕行道每2帧固定执行一次全图双模型车牌链路；冲洗道仅在主检测存在车辆候选时执行，因此两路管线FPS存在明显差异。
- 2026-08-31 RKNNLite运行时确认 `plate_detect.rknn` 和 `plate_rec_color.rknn` 均为 FLOAT16，不是 INT8。
- 本次实测发现type2可能早于type1；迟到的type1会留在上传缓冲区，无法被已经发生过的type2再次flush。

## 4. 解码与RGA硬约束

### 4.1 主路RTSP唯一允许链路

```text
RTSP
  -> GStreamer rtspsrc
  -> depay / parse
  -> mppvideodec
  -> video/x-raw, format=BGR
  -> appsink
```

- 主路 RTSP 只允许 GStreamer `mppvideodec format=BGR` direct-BGR。
- 该路径由 gst-rockchip 内部通过 fd/DMA-BUF 使用 RGA，是当前唯一经过实机验证的允许方式。
- GStreamer direct-BGR 打开失败时，主路 RTSP 按读流失败处理。
- 主路 RTSP 不允许回退到 FFmpeg 硬解。
- 主路 RTSP 不允许回退到 GStreamer `videoconvert` safe 管线。

### 4.2 离线文件例外

- 离线测试视频可以保留不使用RGA的解码方式，以支持问题复现和自动回归。
- 离线文件链路不得恢复任何显式 RGA resize 或 `scale_rkrga`。
- 离线回放与现场 RTSP 必须在代码和测试中明确区分，不能共用模糊的 `auto` 回退语义。

### 4.3 永久禁止项

- 禁止恢复 `ffmpeg_rga`、`scale_rkrga`。
- 禁止恢复 `wrapbuffer_virtualaddr`、`imresize` 等虚拟地址RGA路径。
- 禁止在未完成独立实机错误路径验证前引入新的显式RGA调用。
- 禁止在 Web 操作员页面开放解码后端和RGA开关。

## 5. 产品与事件口径

- 甲方产品语义为“一车一次冲洗”。
- 一次物理车辆生命周期对应一个稳定业务 `event_id`。
- type1～type6可继续作为阶段更新，但同一车辆的所有阶段、纠错和重发必须使用相同 `event_id`。
- 甲方接口暂不增加 `revision` 或 `idempotencyKey` 公共字段；板端可维护内部 revision。
- 甲方平台必须将 `id` 作为更新同一记录的业务键。若同ID仍新增记录，上报层改造暂停并先完成甲方接口协调。
- tracker ID只是算法观测ID，不能继续承担最终业务ID职责。
- 行驶正向和反向均允许；`flow_vector`只负责方向分类，不作为事件准入条件。

## 6. 改造路线图

### M0：冻结解码边界与修复基线漂移

- [x] 将两份运行配置的 `decode_backend` 固定为 `gstreamer`，并由运行时代码校验。
- [x] 主路RTSP只尝试GStreamer direct-BGR，打开失败后直接返回读流失败。
- [x] 删除主路RTSP的FFmpeg回退和GStreamer safe管线回退入口。
- [x] 保留离线文件所需的非RGA解码能力，并与RTSP分支明确隔离。
- [x] 不删除 `tests/test_site_config_runtime.py`。
- [x] 将该测试中的旧 `decode_backend=auto` 断言更新为 `gstreamer`。
- [x] 保留NPU核心分配、冲洗/绕行配置隔离、车轮开关等站点保护断言。
- [x] 新增“RTSP的GS失败后不得调用FFmpeg”的回归测试。
- [x] 新增“RTSP只能使用direct-BGR”的回归测试。
- [x] 保留“离线文件可使用非RGA解码”的回归测试。
- [x] 修正 README、AGENTS和配置说明中仍存在的 `auto -> FFmpeg` 旧表述。
- [x] 记录输入20 FPS、处理17～19 FPS的统一采集方法和板端基线证据。

#### M0实施证据（已确认）

```text
状态：已完成并通过人工确认
完成日期：2026-08-31
关联 commit：本次M0提交，以Git历史记录为准
自动测试：
  - Windows定向测试：40 passed
  - Windows完整测试：194项，186通过；8项为既有Windows/Linux平台差异
  - RK3588定向测试：40项全部通过
  - RK3588完整测试：194项全部通过
RK3588/现场验证：
  - 设备：RK3588-Office-02
  - 目录：/home/serve/cleaningcar_v2_latest
  - ConfigManager解析结果：config.json=gstreamer/direct，config_绕行.json=gstreamer/direct
  - gst-inspect确认mppvideodec支持BGR及video/x-raw(memory:DMABuf)
  - 初始验证未启动run_zone_detect；随后按授权完成双路RTSP实测并主动停止两路推理
性能基线采集方法：
  - 读取推理日志中每10秒输出的[perf]记录
  - 同时记录解码FPS、管线FPS、worker FPS和平均推理耗时
  - 排除启动、重连、文件突发读取等非稳态窗口
历史证据：
  - 正常窗口解码/管线约17～20 FPS
  - worker单帧推理约19～20 ms
  - 当前阶段以稳定性和事件正确性优先，不以强制追满20 FPS为目标
M0双路RTSP实测：
  - 日期：2026-08-30
  - 设备：RK3588-Office-02
  - 两路同时运行约5分53秒，分别采集34个10秒稳态窗口
  - 冲洗道：解码FPS平均24.94、最小23.24、最大25.10；管线FPS平均24.94、最小23.14、最大25.10
  - 冲洗道：worker推理平均19.97 ms；最终0丢帧、0主流重连、0事件
  - 绕行道：解码FPS平均24.73、最小17.57、最大25.14；管线FPS平均16.82、最小12.68、最大17.72
  - 绕行道：worker推理平均19.82 ms；最终心跳累计2727丢帧、0主流重连，任务队列持续满载
  - 解码链日志：GStreamer direct-BGR出现3次，safe-BGR为0次，FFmpeg reader为0次，主流重连为0次
  - 结论：M0解码链能够跟上约25 FPS输入；绕行道约16～17 FPS瓶颈位于解码后的算法/队列阶段
  - 测试副作用：绕行道生成52个事件JSON；type1=12，type2=10，type4=10，type5=10，type6=10
  - 上报记录：本次测试49条进入已发送日志，上传队列最终为0；这些测试数据不自动清理
  - 甲方页面核验：http://47.105.123.104:60006/local/pvehicleDataInfoList 已看到本次记录，确认49条已实际到达平台
  - 人工复核：本次52条绕行事件属于纯模型误检，样本需作为困难负样本加入训练/验证数据集并复核类别阈值
  - 测试结束后两路推理均已停止，调试Web继续运行并已加载M0模块
遗留问题：
  - Windows完整测试的8项既有失败不属于M0：4项依赖os.geteuid，3项依赖killpg/SIGKILL，1项为Windows路径格式差异
  - 最新代码尚未迁移覆盖生产旧副本
  - 两路同秒启动时共用同一个推理日志文件名，存在日志内容交织风险，需要后续按配置命名空间修复
  - 绕行道持续背压、丢帧及短时间大量事件不属于M0解码问题，进入M1～M3继续治理
确认人：项目负责人于2026-08-31确认M0完成
```

### M1：建立一车一次业务生命周期

- [ ] 在跟踪器和事件状态机之间增加独立的业务生命周期层。
- [ ] 业务 `event_id` 与 tracker ID解耦，并记录一个事件关联过的所有tracker ID。
- [ ] 生成跨进程重启不冲突的稳定业务ID。
- [ ] 将事件超时从帧数换算改为基于采集时间的秒级判断。
- [ ] 检测消失后保留8秒续接宽限窗口。
- [ ] 修复事件状态80帧失效、跟踪状态100帧仍可恢复导致的生命周期重建。
- [ ] 锁定车牌一致时优先续接原业务事件。
- [ ] 无有效车牌时，使用时间、位置、区域阶段、车型和运动趋势进行一对一续接。
- [ ] 禁止一个旧业务事件同时认领多个新tracker ID。
- [ ] 支持从本地事件库恢复进程重启前未结束的生命周期。
- [ ] 异常消失超过8秒后保留已有证据，以异常原因结单，不直接丢弃。
- [ ] 跟踪器使用采集时间或真实帧间隔计算预测步长，不能在跳过多帧后只执行一次固定步长Kalman预测。
- [ ] 将tracker丢失年龄和事件超时统一改为秒级语义，避免丢弃帧继续消耗“可观测命中次数”。
- [ ] 增加25 FPS输入、约16～17 FPS处理且连续丢旧帧的跟踪回归测试，验证遮挡、边缘车辆和快速车辆不因帧号跳跃频繁换ID。

#### M1第一批小步改造证据（四场景初测通过）

```text
状态：代码、自动测试和config1～4首轮视频手测完成；M1完整业务ID层尚未实施
完成日期：2026-08-31
本批范围：
  1. tracker丢失保留按源FPS换算，默认8秒；25 FPS时tracker保留200帧、事件状态保留225帧
  2. 事件清理显式尊重ByteTrack/Legacy tracker仍保留的tracked和lost ID，避免同ID恢复前事件状态被删除
  3. type1单独出现仍只进入上传缓冲区；type2成立时若缺type1则先补齐type1，再按同ID顺序发送type1、type2
  4. type3/type4等可选阶段不强制补造；已进入高阶段后出现低阶段时抑制对外发送并记录sequence_issue
  5. 新增默认关闭的事件输入追踪，输出run.json、frames.jsonl、events.jsonl和summary.json
自动测试：
  - Windows定向测试：68项全部通过
  - Windows完整测试：200项中192项通过，8项仍为既有Windows/Linux平台差异
  - RK3588定向测试：68项全部通过
  - RK3588完整测试：200项全部通过
RK3588验证：
  - 设备：RK3588-Office-02
  - 两份配置均解析为track_lost_grace_seconds=8.0、tracker=200帧、event=225帧
  - event_trace_enabled默认false，测试未启动两路RTSP推理
  - 调试Web正常，两路推理均保持停止
手测说明：docs/事件层输入追踪与手测说明.md
四场景初测：
  - 修复Zone坐标缩放与EOF顺序后，config1～4均形成同ID有序事件1、2、4、5、6
  - 四次运行sequence_issue均为0，type1、2、4、5进入带图上传，type6最后发送
  - 项目负责人初步确认平台显示结果比较正常
  - 四段视频均无水流模型输出，因此未验证type3
本批未处理：
  - 跨tracker ID合并和稳定业务event_id
  - Kalman按真实时间差进行多步或变步长预测
  - 进程重启恢复未结束生命周期
  - 模型误检、车辆阈值和双车牌性能
确认人：项目负责人于2026-08-31确认四场景初测结果正常；整个M1仍待完整业务ID层和复杂场景验证
```

### M2：Zone A/B空间状态机与边缘误触发治理

- [x] 本阶段不引入门线配置，保留现有Zone A、Zone B和flow vector三类空间输入。
- [x] 使用 `cv2.pointPolygonTest(..., measureDist=True)` 计算锚点到Zone A边界的有符号距离，将Zone A拆分为明确外部、边界缓冲带和内部核心区。
- [x] Zone A边界缓冲距离按车辆框高度动态计算，初始策略为车辆框高度的10%，并限制在4～24像素，避免远近车辆共用固定像素阈值。
- [x] 管线向ZoneManager传递车辆框高度或等价尺度，ZoneManager不得仅依赖单个无尺度锚点进行边界判定。
- [x] Zone A增加观察防抖：明确在外部连续3次有效观测后进入 `OBSERVED_OUTSIDE`。
- [x] Zone A增加进入防抖：轨迹经过边界缓冲带并连续3次位于核心区后，才进入 `INSIDE_A_CANDIDATE`。
- [x] Zone A增加退出防抖：曾进入核心区的轨迹连续5次明确位于Zone外后，才确认正常离场。
- [x] 以上计数使用实际处理到的有效观测次数，不使用跳跃的原始frame_idx直接消耗防抖门槛。
- [ ] 新track第一次有效观测已位于Zone A核心区时标记 `born_inside_a=true`，只建立本地候选，不立即生成正式type1。
- [ ] born-inside候选后续稳定进入Zone B时，将Zone B视为强业务证据，交由M1先补齐type1再顺序生成type2。
- [ ] born-inside候选未进入Zone B便离开Zone A时，关闭为 `PASS_BY` 或 `REJECTED`，保留trace但不上传正式冲洗事件。
- [ ] 轨迹在Zone A内部检测消失时交由M1保留8秒；超时后按异常消失结单，禁止伪装为正常type5离场。
- [ ] Zone B继续负责清洗区占用和停留，保留进入/退出防抖，并增加“Zone B应完整位于Zone A内部且保持安全间距”的配置告警。
- [x] 方向判断使用H最近多帧在flow vector上的净投影变化，不再仅依赖单个进入点和退出点；正向、反向均允许，净位移不足时标记方向未知。
- [x] 废除“底部点再固定反向移动0.3×车辆框高度”作为唯一业务锚点；L仅保留为兼容回退，D升级为正式业务锚点。
- [x] 定义统一方向锚点结果：H、D、L、方向置信度、锁定状态和渐变进度，Zone状态机只消费selected point。
- [x] 正式锚点以H为中性基点，结合配置flow和车辆实际行进方向生成D；方向未知时D=H，锁定后5帧平滑过渡。
- [ ] 检测框触碰画面底边或左右边缘时标记为截断框；近景截断期间优先使用最近可靠锚点、tracker预测和轨迹趋势，禁止直接把画面边缘当作车辆地面位置。
- [ ] 远景小框保持较小修正，近景大框自动降低框高比例权重；具体曲线使用事件trace中的框高、锚点和人工Zone结果标定，不凭单个视频固定。
- [ ] Web调试画面同时显示车辆框、原始底部中心、最终业务锚点、锚点可信度和最近轨迹，使甲方调整Zone时看到的就是实际判定点。
- [x] Web提供方向感知正式模式和旧兼容回退模式；H/D/L调试对照及方向窗口参数放在开发者参数。
- [ ] Web Zone编辑器显示Zone A边界缓冲预览，并在Zone A顶点距离画面边缘小于3%时提示“缺少外部观察带”，但不强制拒绝保存。
- [ ] Web配置检查Zone B越界、Zone A/B间距过小及Zone覆盖长期停车区域等风险，并以告警而非硬阻断方式提示。
- [ ] trace新增Zone空间状态、signed distance、动态margin、born_inside_a、观察计数和状态变化原因，支持逐视频人工核对。
- [ ] 将“真实车辆但不应进入业务区域”和“画面中不存在车辆的纯模型误检”分开统计，前者由Zone状态机/生命周期处理，后者进入模型困难负样本集。

#### M1与M2联动规则

```text
M1负责：身份连续、tracker保留、事件顺序、8秒等待、trace基础
M2负责：空间状态、区内出生、进入/离开确认、方向和Zone业务有效性

正常轨迹：
  Zone外连续观察
    -> Zone A边界缓冲
    -> Zone A核心候选（type1本地缓存）
    -> Zone B确认（M1保证type1、type2顺序）
    -> 离开Zone B
    -> Zone A外连续确认（type5）
    -> type6结束

区内出生：
  born_inside_a候选
    -> 进入Zone B：M1补type1后生成type2，升格正式生命周期
    -> 未进Zone B直接离开：PASS_BY/REJECTED，仅保留trace
    -> Zone内消失：M1等待8秒，恢复则继续，超时则异常关闭
```

- [ ] M2不得修改车辆模型阈值来掩盖空间状态问题；模型误检治理继续留在M3。
- [ ] M2不得自行创建第二套tracker保留或事件顺序规则，所有丢失等待和阶段单调统一复用M1。
- [ ] M1的候选/正式业务生命周期未来应以M2输出的空间状态作为升格证据，但空间证据不足不得删除原始观测和trace。
- [ ] M1和M2共同保证“暂不上报不等于不保存”：候选、拒绝和异常生命周期均可在本地追溯。

#### M1/M2前置硬伤修复与四场景重跑证据

```text
完成日期：2026-08-31
确定性修复：
  1. Zone/flow归一化坐标允许±0.001浮点误差，-0.0002和1.0002钳制到0/1后再缩放
  2. ConfigManager加载、运行时scale_polygon/scale_point和Web保存三层防止近单位坐标失效
  3. 文件结束时先flush并补齐type4/type5，再由回调发送绝对终态type6
自动测试：
  - Windows定向测试：70项全部通过
  - Windows完整测试：205项中197项通过，8项仍为既有Windows/Linux平台差异
  - RK3588定向测试：70项全部通过
  - RK3588完整测试：205项全部通过
修复前：
  - config1三次：主track稳定384帧，几何应命中Zone A约324～333次，运行时Zone A=0，无事件
  - config2合法Zone运行：1、2、5、6，type4因晚于type5被抑制；含-0.0002的两次运行无事件
  - config3：1、2、4、6，文件结束先发type6导致type5被抑制
  - config4含-0.0002运行：主track稳定115帧，几何应命中Zone A 88次，运行时Zone A=0，无事件
修复后四场景重跑：
  - config1：Zone A=333、Zone B=298，事件1、2、4、5、6，顺序异常0
  - config2：Zone A=1009、Zone B=950，事件1、2、4、5、6，顺序异常0
  - config3：Zone A=576、Zone B=376，事件1、2、4、5、6，顺序异常0
  - config4：Zone A=88、Zone B=56，事件1、2、4、5、6，顺序异常0
  - 四个业务ID均实际进入已发送日志；type1、2、4、5带captureImage，type6无对外图片
  - 四段视频水流输入均为0，因此没有type3；该结果不属于Zone或清理时序故障
结论：坐标缩放和EOF终态顺序两项硬伤已通过同一批视频前后对照验证。
```

#### M2当前实施进度

```text
已完成的M2前置条件：
  - Zone/flow近单位坐标容差与钳制
  - Web保存坐标钳制
  - 文件结束type4/type5/type6顺序
  - 事件输入trace和四场景基线
  - config1～4现有Zone在简单状态机下均可生成正常1、2、4、5、6
已完成并通过板端回放验收的M2主体：
  - Zone A有符号距离和动态边界缓冲
  - 3/3/5有效观测防抖
尚未实施的M2主体：
  - born_inside_a、PASS_BY、REJECTED状态
  - Zone内消失与M1八秒等待的正式联动
  - Web真实锚点预览的产品化收口
  - Zone B越界、安全间距和画面边缘告警
结论：当前四场景正常证明前置硬伤已消除，不代表M2空间状态机已经完成。
```

#### 双锚点影子比较实施证据

```text
历史状态：本轮用于比较legacy与adaptive，正式事件当时仍使用legacy；后续正式结论见H/D/L章节
完成日期：2026-08-31
实现内容：
  - AnchorResult统一输出legacy/adaptive/selected、confidence、truncated_edges和历史复用状态
  - adaptive使用地面接触候选，垂直内缩为min(0.08×框高, 0.02×画面高度)
  - 检测框触碰画面边缘时标记截断；存在可靠历史时使用限幅运动预测
  - debug画面与annotated单车视频同时绘制L/G、双轨迹、正式白圈、距离、可信度和截断边缘
  - Web开发者参数支持legacy/adaptive正式模式和影子比较开关
自动测试：
  - Windows锚点相关定向测试75项全部通过
  - Windows完整测试210项中202项通过，8项仍为既有平台差异
  - RK3588定向测试75项全部通过
  - RK3588完整测试210项全部通过
四场景影子重跑：
  - config1：L/G平均距离216.7px、最大247.7px；检测框384/384帧截断，无可靠历史复用
  - config2：L/G平均距离334.8px、最大458.9px；检测框760/1009帧截断，历史复用27帧
  - config3：L/G平均距离145.1px、最大237.6px；截断122/733帧，历史复用14帧
  - config4：L/G平均距离140.5px、最大192.3px；截断31/115帧，无历史复用
  - legacy仍驱动正式事件，四场景事件结果不因影子锚点改变
单车调试录像：
  - config1：/data/ftp/per_id/20260831/13/RK3588-DEV1-202608311358-1.mp4
  - config2：/data/ftp/per_id/20260831/13/RK3588-DEV2-202608311359-1.mp4
  - config3：/data/ftp/per_id/20260831/14/RK3588-DEV3-202608311401-1.mp4
  - config4：/data/ftp/per_id/20260831/14/RK3588-DEV4-202608311402-1.mp4
结论：
  - 该轮双锚点可视化和trace为后续H/D/L方案提供了人工比较依据
  - adaptive当前不能切正式，尤其config1/2从首帧开始即为近景截断大框，缺少可靠历史启动点
  - 下一步先设计截断场景的启动/地面约束，再决定adaptive参数；Zone状态机继续暂缓
```

#### H/D/L方向锚点验证（已确认正式启用）

```text
正式口径：Zone使用D，L仅保留调试对照和紧急回退
H：车框底部上移anchor_offset_ratio×框高，不做flow偏移
L：旧正式锚点，H沿配置flow反方向偏移anchor_legacy_flow_shift_ratio×框高
D：根据H最近多帧沿flow的投影位移判断正/反向；未知时D=H，锁定后分5帧渐变
G：暂时停用，不参与画面和业务判定；anchor_update暂时保留兼容字段
验证结果：板端215项测试通过；config1/2/3稳定为forward，config4稳定为reverse，用户确认采用方向感知方案
正式切换回放：
  - config1：1480帧，20.48 FPS，0丢帧，事件1→2→4→5→6
  - config2：2925帧，20.54 FPS，0丢帧，事件1→2→4→5→6
  - config3：950帧，18.08 FPS，0丢帧，事件1→2→4→5→6
  - config4：318帧，21.40 FPS，0丢帧，方向为reverse；仍复现type5先于type4，晚到type4被阶段回退抑制
性能结论：方向感知仅维护每车约8个H点并做标量投影、符号一致率和5帧插值，不增加模型推理，实测未造成可见FPS下降
下一步：M2空间防抖已完成板端测试和config1～4回放，并由项目负责人确认检查点
```

#### M2空间防抖检查点（已确认）

```text
实施日期：2026-08-31
实现内容：
  - 彻底退役早期Zone A mask：删除管线mask创建、Worker涂黑、配置默认值和Web开发者字段；旧配置中的zone_a_mask_enable加载后主动移除
  - Zone A使用pointPolygonTest有符号距离，动态margin为车辆框高度10%，限制4～24像素
  - 新增按唯一有效观测计数的外部3次、核心区3次、明确离开5次防抖；跳跃frame_idx不直接消耗计数
  - 缓冲带不加次数，保留最后明确区域的候选计数并清除对向候选
  - 首见核心区暂走3次确认的兼容路径并记录initial_core_compat；born_inside_a、PASS_BY、REJECTED仍留待下一检查点
  - type5在Zone A确认退出后进入pending；Zone B仍在时不发送、不关闭生命周期，Zone B结束后保持type4先于type5
  - trace记录空间状态、signed distance、动态margin、3/3/5计数、状态变化原因及type5挂起/释放/取消原因
配置参数：
  - zone_a_margin_ratio=0.10
  - zone_a_margin_min_px=4，zone_a_margin_max_px=24
  - zone_a_observed_outside_hits=3，zone_a_enter_core_hits=3，zone_a_exit_outside_hits=5
  - 参数进入ConfigManager校验和正式JSON，但不进入Web参数页面
Windows验证：
  - M2、事件、配置、Worker相关定向测试76项和4个子测试全部通过
  - 完整测试222项中214项通过；8项仍为既有Windows/Linux平台差异，无本检查点新增失败
RK3588板端验证：
  - M2、事件、配置、Worker相关定向测试76项全部通过
  - 完整222项全部通过
  - config1：1480帧，20.48 FPS，0丢帧，事件1→2→4→5→6
  - config2：2925帧，20.54 FPS，0丢帧，事件1→2→4→5→6
  - config3：950帧，18.06 FPS，0丢帧，事件1→2→4→5→6
  - config4：318帧，21.49 FPS，0丢帧，事件由原1→2→5→6修正为1→2→4→5→6
trace结论：
  - config2在检测结束前得到2次明确Zone A外部观测，config4得到1次，均未伪造为5次退出确认
  - 证据不足时不再由单帧触发type5，而是保留生命周期并在文件EOF清算时按type4→type5顺序结束
  - 真实连续5次外部观测、重复frame_idx不计数、缓冲带中断和type5等待Zone B退出均由板端自动测试覆盖
确认状态：项目负责人确认3/3/5防抖改动可作为检查点提交
运行状态：四路推理均已停止，调试Web重启后继续监听8000
```

### M3：模型结果到业务事件绑定

- [ ] 明确车辆框、车牌框、水流框、Zone A、Zone B之间的数据关系和所有权。
- [ ] 水流检测必须通过空间关系绑定到具体车辆。
- [ ] 同时存在多辆车时，禁止一个水流框无条件触发所有轨迹。
- [ ] 正式启用多帧水流窗口和连续命中门槛。
- [ ] 核对 `stationary_min_frames`、`type34_min_interval_frames` 等现有配置是否真正参与逻辑。
- [ ] 删除不再需要的无效配置，避免Web显示“可调但不生效”的参数。
- [ ] 明确type1～type6的内部含义、触发条件、异常路径和字段快照时机。
- [ ] 使用现场边缘驻留、遮挡、换ID、反向行驶和水流干扰视频建立回放样本集。
- [ ] 所有阈值先通过现场视频回放确定，再进入默认配置。
- [ ] 将2026-08-30绕行误触发对应帧、截图和上下文视频整理为纯模型误检困难负样本，并保留空标注/正确类别标注依据。
- [ ] 按类别统计误检置信度分布，通过PR曲线评估现有 `0.8` 车辆阈值，不直接凭单次现象提高阈值。
- [ ] 将主检测、车牌检测、车牌识别/颜色、CPU前后处理和事件处理分别计时，避免继续用主检测 `infer_ms` 代表整条管线。
- [ ] 为 `plate_detect.rknn` 和 `plate_rec_color.rknn` 建立独立板端基准：单模型延迟、双模型触发耗时、峰值内存和NPU核心负载。
- [ ] 基于代表性车牌校准集分别转换并验证两个INT8候选模型，与当前FLOAT16模型比较检测召回率、整牌准确率、颜色准确率和板端延迟。
- [ ] 量化模型未达到既定精度门槛时保留FLOAT16回退，不以牺牲车牌有效识别率换取表面FPS。
- [ ] 将绕行道“每2帧全图车牌推理”改为业务触发策略：无有效车辆候选时不推理，车辆生命周期内按首检、重试和锁定状态控制频率。
- [ ] 评估在车辆ROI内执行车牌检测，比较全图与车辆ROI方案的漏检、误绑和总耗时。
- [ ] 车牌颜色候选必须绑定规范化车牌文本或稳定plate binding，禁止所有文本共享同一个滚动颜色池。
- [ ] 车牌文本锁定后，只允许与锁定文本一致的颜色候选参与锁定和切换；空文本及其他错误文本不得覆盖已锁颜色。
- [ ] 为每个车牌文本维护独立颜色证据，正确颜色不得仅因最近50帧未再次看到车牌而失效；颜色切换必须来自同一文本的连续强证据。
- [ ] 锁定文本缺少可靠颜色时才允许使用车型启发式fallback；`yellow truck`应回退为黄色，已被错误颜色锁定时不得阻断合理纠错。
- [ ] 增加config2颜色回归样本：`苏A3A329`高置信度颜色89次黄色、1次蓝色，最终必须输出黄色，不能被后期`苏B0JS28`、`豫N0T7RUK`或空文本的蓝/绿/黑候选覆盖。
- [ ] trace记录原始颜色候选所属文本、颜色分桶、锁定/切换原因和最终颜色来源，支持区分颜色模型误识别与事件层聚合错误。

### M4：每车道独立事件库与可靠上报

- [ ] 冲洗道和绕行道分别建立独立SQLite事件库并启用WAL。
- [ ] 建立事件主表、阶段历史、tracker别名、人工修订、审计日志和上传outbox。
- [ ] 同一事件阶段和同一内部revision只能产生一个outbox任务。
- [ ] 修复当前上传缓冲区使用不同业务键的问题。
- [ ] 自动重试和人工重发始终复用原业务ID。
- [ ] 板端保存内部revision，但不向甲方增加新字段。
- [ ] 网络请求处于“服务端可能成功、客户端未收到响应”状态时，禁止板端无限生成新任务。
- [ ] 将上传状态拆分为 `buffered`、`queued`、`http_acked` 和 `failed`；`upload_log_sent` 只在收到成功HTTP响应后记录，或重命名为准确的队列日志。
- [ ] 记录HTTP状态码、响应时间和不含敏感信息的业务确认结果，使板端日志可以独立判断是否真正上传成功。
- [ ] 推理日志文件名加入配置命名空间或device_id，避免两路同秒启动写入同一个日志文件。
- [ ] 人工可修订车牌、车型、车牌颜色、方向、冲洗起止、清洁度、有效性和备注。
- [ ] 原始识别值和原始事件证据永久保留，不允许被修订值覆盖。
- [ ] 默认保留90天数据。
- [ ] 磁盘使用率超过85%时，仅清理已结束且已成功上报的最旧事件。
- [ ] 待确认、上报失败和未结束事件禁止自动清理。

### M5：事件台账展示层

- [ ] 第一版只做事件台账和复核，不开发实时视频大屏。
- [ ] 支持日期、车牌、车型、事件状态、有效性和上传状态筛选。
- [ ] 支持分页，避免直接扫描全部历史文件。
- [ ] 事件详情展示阶段时间线、事件截图、左右车轮结果和异常原因。
- [ ] 展示原始识别值与人工修订值的差异。
- [ ] 支持标记有效、误报、待确认并填写备注。
- [ ] 支持允许字段的人工纠错。
- [ ] 支持使用原业务ID重发最终完整状态。
- [ ] 查看页面可保持只读开放；纠错和重发必须经过本地管理员登录。
- [ ] 所有纠错和重发操作记录操作者、时间、修改前后值和执行结果。

### M6：Web配置层收口

- [ ] 将Web拆分为操作员页面和默认隐藏的开发者页面。
- [ ] 操作员页面只保留启停、设备/车道、主路及车轮源、接口地址、Zone A/B和flow vector。
- [ ] 解码、RGA、模型路径、NPU核心、算法阈值和生命周期超时移入开发者页面。
- [ ] GStreamer direct-BGR等硬约束由运行时代码校验，不能只依赖页面隐藏或置灰。
- [ ] 删除或标记未生效、已废弃及甲方无需理解的配置项。
- [ ] 配置变更记录操作者、时间和修改前后差异。
- [ ] 两个车道的配置、运行状态、事件库和台账查询保持隔离。

## 7. 重点验收场景

- [ ] 静止边缘车辆持续30分钟不产生业务事件。
- [ ] 同一车辆检测消失少于8秒且tracker ID变化时，业务ID保持不变。
- [ ] 连续两辆外观相似且无有效车牌的车辆不会被错误合并。
- [ ] 新track直接出生在Zone A核心区时不立即产生正式type1；进入Zone B后可按同ID补齐type1、type2。
- [ ] born-inside车辆未进入Zone B便离开时只形成PASS_BY/REJECTED候选，不上传正式冲洗事件。
- [ ] Zone A边界单帧抖动或短暂跨界不会触发重复进入、退出或type5。
- [ ] Zone A内部检测消失不会直接产生正常type5；8秒内恢复后继续原生命周期，超时后异常关闭。
- [ ] 正向和反向车辆均可通过多帧flow vector投影得到稳定方向；运动不足时返回方向未知而非强行分类。
- [ ] Zone B超出Zone A、Zone A贴近画面边缘或Zone间距过小时，Web提供明确风险告警。
- [ ] 程序重启后，区域内车辆可以续接未结束事件。
- [ ] 一个物理车辆在甲方平台最终只形成一条可持续更新的冲洗记录。
- [ ] 人工纠错后使用原业务ID重发，本地保留完整原值和审计记录。
- [ ] 网络重试、进程重启和重复点击重发不会在板端生成重复outbox任务。
- [ ] 冲洗道与绕行道的事件数据库、查询、修订和清理互不串数据。
- [ ] 在25 FPS输入且处理约16～17 FPS的压测中，跟踪ID不会因丢旧帧和帧号跳跃产生可观测的系统性分裂。
- [ ] 无车辆画面连续运行5分钟时，绕行道不执行持续全图车牌推理，不产生车辆事件。
- [ ] 双车牌FLOAT16和INT8候选模型具有可重复的分阶段板端性能与精度对比报告。
- [ ] type1～type6阶段顺序符合状态机约束，不再出现type2先发送、type1后进入缓冲区。

## 8. 下一步执行门禁

本文档创建后不自动开始M0。

下一次实施必须由项目负责人明确指定具体事项，例如：

```text
开始 M0：锁死主路RTSP只使用GStreamer direct-BGR，并修正相关测试。
```

完成该事项后，实施者提交代码差异、自动测试和板端验证证据，再等待人工确认是否勾选。
