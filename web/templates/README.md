# web/templates 目录说明

这个目录存放 Web 页面模板。

## 当前文件

- `zone_editor.html`
  - 当前 Web 控制台主页面
  - 包含配置加载、ROI 编辑、推理控制、日志查看、手动获取调试帧等页面逻辑
  - 当前不再包含按车辆 ID 录像浏览区域

## 使用方式

- 由 `web/server.py` 加载
- 如果访问首页返回模板缺失，优先检查这个目录和文件是否存在
- 页面切换场景配置时不会自动抓背景帧；背景帧和调试帧都需要显式点击获取
